version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - echo "installing something"
  build:
    commands:
      - echo: "we are in the build block"
      - echo: "we will run some tests"
     
  
